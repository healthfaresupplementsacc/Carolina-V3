node_modules/
.env
public/archive/*.png
*.log
.DS_Store

# dados locais (dumps de prod, config pessoal)
snapshots/
.claude/settings.json
analysis_output.json

# bundle de design (referência local pesada — 24MB; fora do git/deploy)
docs/design/handoff/
docs/design/screenshots/

# drift check (weekly): local log + dated drift reports (regenerated, not tracked).
# NOTE: pattern is date-scoped so docs/DRIFT-SETUP.md stays tracked.
drift-log.txt
docs/DRIFT-[0-9]*.md
