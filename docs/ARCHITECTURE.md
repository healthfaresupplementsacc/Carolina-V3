# HealthFare Tracker — Architecture Map

Generated by tracing from `src/index.js`. Every claim cites file and line.
Excludes `scripts/`, `public/`, `docs/investigacao/_raw/`. No deletions recommended.

Two generations run side by side: a legacy layer (`src/`) and a V3 layer
(`src/v3/`), bridged by `src/v3/wire.js`. See `CLAUDE.md` for which to use per concern.

---

## 0. What actually runs (from `src/index.js`)

`src/index.js` is the only entry point (`npm start`). Boot order:

**Routers mounted** (in order, `src/index.js`):
1. `src/slack/events` router at `/` — `src/index.js:45-46` (legacy Slack Events API + Interactivity).
2. `src/routes/api` at `/api` — `src/index.js:47`.
3. `src/routes/workflow` at `/api` — `src/index.js:48` (workflow_templates etc).
4. `src/v3/wire.mount(app)` — `src/index.js:51` → mounts all V3 routes (see below).
5. `GET /` → 302 redirect to `/dashboard-v4/` — `src/index.js:57`.
6. `src/routes/cameras` at `/` — `src/index.js:61` (PIN-gated camera proxy).
7. `src/dashboard/router` at `/` — `src/index.js:63` (legacy server-rendered dashboard + its APIs).

**V3 routes mounted inside `src/v3/wire.js` `mount()`:**
- `POST /slack/events-v2` — `wire.js:96` (V3 Slack webhook).
- security headers + brute-force gate on `/op`, `/admin`, `/api/v3/op`, `/api/v3/architect`, `/api/adminpanel` — `wire.js:116-128`.
- `src/v3/admin-v3/routes` (shadow inspection `/api/admin/v3/*`) — `wire.js:131`.
- `src/routes/architect` (read-only architect API) — `wire.js:136`.
- `src/routes/op` (operator page API `/api/v3/op/*`) — `wire.js:149`.
- static: `/shared`, `/op`, `/print` — `wire.js:160-163`.
- `src/routes/admin` (admin panel API) — `wire.js:166`; static `/admin` — `wire.js:172`.
- `src/v3/data/router` (dashboard JSON API `/api/v3/data/*`) — `wire.js:200`.
- static SPAs: `/dashboard` — `wire.js:206`; `/dashboard-v4` — `wire.js:216`.
- `/foto` static + `src/v3/images/router` (`/api/images/upload`) — `wire.js:231-232`.

**Boot-time jobs** (`src/index.js` `start()`):
- DB migrations — `src/index.js:70` (`db.migrate()`).
- seed message_variations — `:74`; cleanup stale breaks — `:77`; audit-log TTL — `:80`.
- load custom supplements into parser — `:83`.
- seed workflow templates — `:88-89`; legacy→ISA-88 migration — `:99-100` (skippable via `SKIP_LEGACY_MIGRATION=1`).
- Slack connection check — `:110-112`; historical backfill (async) — `:119-128`.

**Legacy crons** (gated off by `V2_DISABLED=1`, `src/index.js:136-154`):
- `startPolling()` — Slack poller (`:141`).
- `startEodJob()` — end-of-day (`:144`).
- `startGreetingJob()` — morning greeting (`:147`).
- `startDetectJob()` — autonomous detection, 30min (`:150`).
- `startActivityCheckJob()` — activity freshness, hourly (`:153`).

**V3 worker + crons** (started by `src/v3/wire.startWorker()`, `src/index.js:164`):
- **Workers watchdog** — 10min, alerts admin on missing heartbeat (`wire.js:267-298`).
- **Observer** (shadow mode) — tick 5s (`wire.js:324-335`).
- Conditional workers (each behind an env flag; see `wire.js`):
  - `proactive-alerts` (`WORKER_PROACTIVE_ALERTS_ENABLED`, `wire.js:339`)
  - `carolina-forgotten-dm` (`WORKER_FORGOTTEN_DM_ENABLED`, `wire.js:353`)
  - `sandbox-cleanup` (default on, `wire.js:377`)
  - `ems-activity-sync` (default on, `wire.js:386`)
  - `veeqo-order-sync` (`WORKER_VEEQO_ORDERS_ENABLED`, `wire.js:400`)
  - `stock-alerts` (`WORKER_STOCK_ALERTS_ENABLED`, `wire.js:423`)
  - `veeqo-mergeable-alert` (`WORKER_MERGEABLE_ALERT_ENABLED`, `wire.js:435`)
  - `print-divergence-watchdog` (`WORKER_PRINT_DIVERGENCE_ENABLED`, `wire.js:448`)
  - `stock-gap-alert` (`WORKER_STOCK_GAP_ALERT_ENABLED`, `wire.js:464`)
  - `unusual-sku-watch` (`WORKER_UNUSUAL_SKU_ENABLED`, `wire.js:484`)
  - `veeqo-dup-shipment-detector` (`WORKER_DUP_SHIPMENT_ENABLED`, `wire.js:497`)
  - `attendance-sync` (default on, `wire.js:510`)
  - `production-total-worker` (default on, `wire.js:525`)
  - `absence-alert` (always attempted, `wire.js:564`)
  - `encap-monitor` (always attempted, `wire.js:572`)
  - `dedupe-watcher` (`WORKER_DEDUPE_ENABLED`, `wire.js:606`)
- Interval crons in `startWorker`: action_log cleanup daily (`wire.js:368`), metrics matview refresh 10min (`wire.js:578`), session cleanup hourly (`wire.js:585`), pending_commands expiry 60s (`wire.js:619`).

**Master kill-switch:** `CAROLINA_SILENT_MODE` (default ON unless `=false`) suppresses
all autonomous Slack posting from workers — `wire.js:313,322`.

---

_Domains below are filled in incrementally. Each cites owning files, tables read/written,
and cross-domain inputs/outputs._

---

## 1. Attendance

Clock punches (NGTeco), check-in/out, breaks/lunch, idle/absence, forgotten-checkout.

**Owning files**
- `src/workers/attendance-sync.js` — core worker: pulls NGTeco punches, drives the day state machine (`attendance-sync.js:6`; NGTeco pull `:104`; punch insert `:428`).
- `src/workers/absence-alert.js` — logged-in-but-idle alert + auto-idle logout (`:3`, `:146`).
- `src/workers/carolina-forgotten-dm.js` — next-day forgotten-checkout reminder (`:17`, reads `forgotten_checkouts` `:51`).
- `src/v3/services/ngteco.js` — NGTeco Office API client (`:100` aggregationDay, `:107` currentDay, `:114` devices).
- `src/v3/presence.js` — reusable "really present today" SQL/JS, excludes EMS phantom (`:29`, `:57`, `:78`).
- `src/v3/attendance-markers.js` — pure function labeling punch pairs (`:32` computeMarkers).
- `src/v3/data/router.js` — `GET /api/v3/data/attendance` (`:1759`) + manual admin-checkout endpoint (`:1906`).
- Wired in `src/v3/wire.js:512-519` (attendance-sync), `:565-566` (absence), `:355-363` (forgotten-dm).

**Tables read / written**
- `v3.att_punch` — WRITE insert `attendance-sync.js:428`; READ in many (`:115,151,292,326,376,459,625`, `carolina-forgotten-dm.js:48`, `router.js:1773`, `absence-alert.js:84`).
- `v3.att_state` — WRITE upsert `attendance-sync.js:514` + updates (`:258,303,342,382,535,562,575,601`) and `router.js:1906-1909`; READ widely.
- `v3.operator_sessions` — WRITE update (logout) `attendance-sync.js:341,568`, `absence-alert.js:146`, `router.js:1917`; READ `presence.js:40,61,84`.
- `v3.forgotten_checkouts` — WRITE insert `attendance-sync.js:347`; WRITE update `carolina-forgotten-dm.js:65,84`; READ `:51`.
- `v3.persons` — READ only (clock_code, slack_user_id, display_name). `v3.operator_schedules` — READ only (`absence-alert.js:61`, `op.js:2174`).
- Also writes: `v3.audit_log` (`carolina-forgotten-dm.js:86`), `v3.operator_action_log` (`absence-alert.js:160,185,211,303`), `v3.notifications` (`absence-alert.js:182`), `v3.ngteco_sync_study` (`attendance-sync.js:447`, observability only).

**Receives from**
- NGTeco hardware (external) via `src/v3/services/ngteco.js` (`attendance-sync.js:104`).
- Tasks domain: reads `v3.events` + `v3.activity_types` to decide presence/idle and lunch slugs (`absence-alert.js:86,93`; `attendance-sync.js:551,583`; `presence.js:45`).
- Login/sessions: reads `v3.operator_sessions` as proof of manual presence (`presence.js:40`).

**Sends to**
- Slack (via injected `src/v3/slack/sender`): admin + operator alerts (`attendance-sync.js:72-82`; `absence-alert.js:150,282`; `carolina-forgotten-dm.js:77,116`; `router.js:1922`).
- Tasks domain: **closes `v3.events`** on checkout/lunch-return/night-sweep (`attendance-sync.js:339,548,564`; `router.js:1913`), excluding machine/long-running (`:565`).
- Sessions: closes `v3.operator_sessions` (`attendance-sync.js:341,568`; `router.js:1917`; `absence-alert.js:146`).
- Alert-gate kill-switch consumed before operator posts (`attendance-sync.js:81`; `absence-alert.js:253`).
- Emits `forgotten_checkouts` rows consumed later by carolina-forgotten-dm (table handoff).

**State location + duplicate writers**
- Primary state: **`v3.att_state`** keyed `(person_id, att_date)`; owner `attendance-sync.js:6`, main upsert `:514`.
- **`att_state` / `checkout_at` is written by exactly TWO files**: `attendance-sync.js` (worker) and `src/v3/data/router.js:1906-1909` (admin manual checkout, deliberately mirrors the worker). `op.js:2166` and `absence-alert.js:239` only READ it. This dual-writer is intentional and consistent (same columns, same downstream event/session closing).
- The login side that CREATES `v3.operator_sessions` is `src/lib/op-auth.js:43` (resolved in a follow-up grep).

---

## 2. Tasks (events)

Operator task lifecycle: start/end/join/retroactive, cowork, pauses, machine handoff. Table `v3.events`.

**Owning files**
- `src/routes/op.js` — operator page HTTP routes: start `:1568` (solo insert `:1766`, cowork inserts `:1717-1732`), retroactive `:1798`, finish-preview `:1959`, end `:2020` (close UPDATE `:2126`), join `:2332`, reclassify `:3070`, machine custody `:2285`.
- `src/v3/services/EventService.js` — documented single write-door for `v3.events` from Slack/Observer/admin (`:5-21`): `upsert` `:542`, `closeActivePersonEvent` `:427`, `safetyAutoClose` `:681`, `_syncCowork` `:364`.
- **IMPORTANT:** `op.js` writes `v3.events` with **raw SQL, bypassing EventService** (`:1725,1766,2126`). The "single write-door" invariant in `EventService.js:5` is NOT enforced against the operator page. (See BROKEN LINKS.)

**Tables read / written**
- `v3.events` — WRITE: op.js (`:1725,1766,1838,1862,2360,2126,2298,2315`), EventService (`:256,210`). READ: op.js `loadOwnedOpenEvent` `:1882`, overlap `:1644`.
- `v3.activity_types` — READ only (`EventService.js:276,296`; op.js `:1887,1583`).
- `v3.audit_log` — WRITE EventService `_audit` `:106`; op.js via `audit()` helper.

**Receives from**
- Attendance closes events directly: `attendance-sync.js:564` (clock_out), `:339` (night_sweep), `:548` (clock_back_in).
- EMS auto check-in/out: `ems-activity-sync.js:326` (insert `ems_auto`), `:228,244` (close). Data via `src/v3/services/ems-api.js:61`.
- Safety EOD close: `EventService.safetyAutoClose:716`.

**Sends to**
- Production counts: op.js `/end` writes `v3.production_counts` (`insertCount` `:2209`, orders `:2251`).
- Slack: `notifyProductionException:1901`, `machineSlack`/`adminSlack:2224`.
- Production-total followup: `openFollowup:1922`.

**State location + duplicate writers**
- **`v3.events.ended_at` is written by FIVE distinct owners** (CONFIRMED): (1) `op.js:1662,1669,1680,2126,1841`; (2) `EventService.js:210` (via `_closeActive:524`, `safetyAutoClose:716`); (3) `attendance-sync.js:339,548,564`; (4) `ems-activity-sync.js:228,244`; (5) also `src/routes/admin.js`, `src/v3/services/CommandHandler.js`, `src/v3/llm/Observer.js`, `src/v3/services/BatchService.js:159,195`, `src/admin/merge.js`, `src/workflow/engine.js`. `absence-alert.js` only READS. (See BROKEN LINKS.)

---

## 3. Production line

Bottle counts, batches/phases, EMS activity sync, encapsulation monitoring, goals, production totals.

**Owning files**
- `src/v3/services/ProductionCountService.js` — canonical writer for `v3.production_counts`: `record:109`, `supersede:243`, `reassign:278`, `softDelete:293`, duplicate incident `_recordDuplicateIncident:204`.
- `src/v3/services/BatchService.js` — `v3.product_batches`: `findOrCreateActive:122`, `closeBatch:133`, `reassignEvents:153`, `mergeBatches:176`.
- `src/v3/services/GoalService.js` — `v3.production_goals` (sole owner): `record:128`, `correct:166`.
- `src/workers/production-total-worker.js` — Slack-thread followup for line-closed-without-total; reads `v3.production_total_followups:70`, updates `:93,117,144,157`.
- `src/v3/production-total-followup.js` — `openFollowup:57` inserts followup; `interpretReply:81` (LLM).
- `src/workers/ems-activity-sync.js` — UPSERTs `v3.ems_activity_cache:201`; also writes `v3.events`, `v3.product_batches:298`, `v3.ems_unconfirmed:336`, `v3.ems_cleaning_log:458`, `v3.products:428,437`.
- `src/workers/encap-monitor.js` — encapsulation-off monitor; reads `v3.events:98`, `v3.operator_schedules:57`, `v3.settings` key `machine_state:160`; writes only `v3.audit_log` dedupe `:207,231`.
- `src/v3/data/flow-views-repo.js` — READ-ONLY aggregation: `productionByDay:125`, `pnpByDay:259`, `fnskuByDay:413`, `reviewRate:485`, `supportByDay:571`.

**Tables read / written**
- `v3.production_counts` — WRITE: ProductionCountService `:86`, op.js `:2209,2251`, admin.js (grep). READ: flow-views-repo, op.js dup/over-target `:2091,2110`.
- `v3.product_batches` — WRITE: BatchService `:95,122`, ems-activity-sync `:298`, op.js/admin.js.
- `v3.products` — WRITE: ems-activity-sync catalog `:428,437`. `v3.production_goals` — GoalService only.
- `v3.ems_activity_cache` — WRITE ems-activity-sync only (`:201,219,330`).
- `v3.data_incidents` — WRITE ProductionCountService `:223`, wire.js `:410`; READ wire.js `_onDataIncident:64`.
- `v3.production_total_followups` — WRITE followup `:57` + worker; READ worker `:70`.

**Receives from**
- EMS API via `src/v3/services/ems-api.js:61` → ems-activity-sync `:386`, op.js `:2381,2391`.
- Camera via `v3.settings` key `machine_state` (pushed by .28 machinemon) → encap-monitor `:160`.
- Operator counts from op.js `/end` (Tasks domain).

**Sends to**
- Slack: encap-monitor `:222,175`, production-total-worker `:50,43`, incidents via wire.js `_onDataIncident:67`.
- Dashboard: flow-views-repo read models via data/router (`wire.js:200`).
- Canonical total write path: `recordTotal` (`wire.js:182`) shared by worker + admin dashboard box → `ProductionCountService.record`.

**State location + duplicate writers**
- **`v3.production_counts` is written by 3 places** (CONFIRMED): (1) `ProductionCountService.js:86` (canonical, dedup/supersede/incident); (2) `op.js:2251` + `insertCount` (operator page, **direct, bypasses the service**); (3) `src/routes/admin.js` (adjustments — UNCERTAIN exact line). The "one write path" comment at `wire.js:180` holds only for the followup-total path, not the operator `/end` inserts.
- `v3.product_batches` is multi-writer too (BatchService, ems-activity-sync `:298`, op.js/admin.js).

---

## 4. Stock

Physical warehouse ledger (bins + boxes), supplies, stock alerts/gaps.

**Owning files**
- `src/v3/services/StockService.js:25` — single write door for physical stock: `storeIn:134`, `pick:178`, `restock:227`, `damaged:286`, `adjust:324`, `count:359`; read `warehouseByProduct:413`.
- `src/v3/services/SupplyService.js:19` — supplies write door: `consumeForSize:65`, `change:103`.
- `src/v3/data/stock-repo.js:9` — READ-only: `bins:13`, `boxes:26`, `summary:39`, `picksheet:97`, `restockList:134`.
- `src/workers/stock-alerts.js:23` — days-of-stock planner + rupture alerts.
- `src/workers/stock-gap-alert.js:20` — P&P stock-gap alerts.
- `src/v3/services/stock-gap-service.js:62` — crosses picklist need × Veeqo stock × EMS.

**Tables read / written**
- `v3.stock_movements` — WRITE (append-only) `StockService.js:77`; **also raw WRITE from op.js kiosk `op.js:324`**. READ `StockService.js:70`, `stock-repo.js:81`.
- `v3.stock_bins` / `v3.stock_boxes` — READ/WRITE `StockService.js:93,106,100,111`; READ `stock-repo.js`, `stock-alerts.js:158,159`, picklist `router.js:1018`.
- `v3.stock_issues` — WRITE `StockService.js:307`. `v3.stock_thresholds` — READ `stock-alerts.js:117`.
- `v3.products` / `v3.product_skus` / `v3.pnp_order_lines` — READ (joins, velocity).
- Supplies: `v3.supply_movements` WRITE `SupplyService.js:85,115`; `v3.supply_items` READ/WRITE `:88,107,118`; `v3.package_size_supply` READ `:55`.
- `v3.audit_log` — WRITE (audit + alert dedupe) `StockService.js:57`, `stock-alerts.js:145`, `stock-gap-alert.js:56`.

**Receives from**
- Orders: shipped lines drive deduction — `veeqo-order-sync.js:124` → `StockService.pick()`.
- Orders (picklist): `StockGapAlert` via injected `getGaps` `stock-gap-alert.js:26,112`; `StockGapService.analyze:111`.
- Printing: `stock-gap-alert.js:64` reads `v3.events` slugs `order_printing`/`order_printing_2` to detect print-station start.

**Sends to**
- Slack (admin-orin + orders channel): `stock-alerts.js:133`, `stock-gap-alert.js:94`.
- data-incidents: StockService `onDiscrepancy` → wire.js `:407` (INSERT `v3.data_incidents`), router.js `:395`. StockService sends nothing to Slack directly; only injected callbacks.

**State location + duplicate writers**
- **`v3.stock_movements` written in TWO places** (the dual-path the header warns about, `StockService.js:6`): (1) `StockService._insertMovement:77` (idempotent `ON CONFLICT (source, source_ref)`, updates bin/box qty in-tx); (2) **raw kiosk INSERT `op.js:324`** (`/api/v3/op/stock/take`, source `'op_kiosk'`) — bypasses the service, no qty update, no idempotency. (See BROKEN LINKS.)
- **UNCERTAIN / dead edge:** `SupplyService.consumeForSize` (label-print → supply-deduct) has **no production caller** — only `SupplyService.change` is wired (admin endpoint `router.js:1384`); `consumeForSize` appears only in tests. The "print label → deduct supply" edge is defined but not wired. (See BROKEN LINKS.)

---

## 5. Orders (Pick & Pack / Veeqo)

**Owning files**
- `src/v3/services/veeqo-api.js:20` — Veeqo client: reads `getOrdersPage:29`, `shippedByDay:60`, `listSellables:127`; the only write `setStock:211` (hardcoded warehouse 108841 `:155`).
- `src/workers/veeqo-order-sync.js:27` — mirrors Veeqo orders into `v3.pnp_order_lines`, deducts stock (live mode).
- `src/workers/veeqo-mergeable-alert.js:18`, `veeqo-dup-shipment-detector.js:20`, `unusual-sku-watch.js:17` — READ-ONLY admin alerts.
- Picklist: `router.js:1009` (`GET /api/v3/data/picklist`); op.js proxies `:293` (picklist), `:303` (stock-gaps). Orders count writer `insertOrdersCount` `op.js:1158`.

**Tables read / written**
- Veeqo = external API (GET `/orders`, `/products`; PUT stock `veeqo-api.js:220`), not a table.
- `v3.pnp_order_lines` — WRITE upsert `veeqo-order-sync.js:52`, `deducted_at:130`. READ picklist `router.js:1037`, unusual-sku `:41`.
- `v3.production_counts` (`kind='orders'`) — WRITE `insertOrdersCount` `op.js:1160` (called `:1739,1788` at task START), `:2252`, adjust/reset `:2914`. READ flow-views `:319,352`, admin `:1138`.
- `v3.product_skus` / `v3.products` — READ.
- `v3.audit_log` — WRITE alert dedupe (mergeable/dup/unusual workers).

**Receives from**
- Stock: picklist reads `v3.stock_bins`/`stock_boxes` for locations `router.js:1016-1023`.
- Printing (P&P print task): order counts originate when operator opens `order_printing` task — `op.js:1738` writes `production_counts kind='orders'` from `orders_printed`.
- Veeqo external: order pages, stock-by-SKU, customer names (`router.js:169,190`).

**Sends to**
- Stock: `veeqo-order-sync.js:124` → `StockService.pick()` (deduct on shipped, live mode; default `'dry'` `:34`).
- Production counts: order-printing task START → `production_counts kind='orders'` (`op.js:1739`), the canonical P&P daily total.
- Slack admin-orin: mergeable `:171`, dup-shipment `:165`, unusual-sku `:85`. Veeqo external: `setStock` PUT `:220`.

**State location + duplicate writers**
- **`production_counts kind='orders'` written in multiple op.js paths, NOT by veeqo-order-sync**: `op.js:1739,1788` (task-start via `insertOrdersCount:1160`), `:2252` (retro), `:2914` (adjust/reset). `veeqo-order-sync.js` writes only `v3.pnp_order_lines`. The P&P "orders printed today" count is operator-typed at print-task start — a *separate* source from the Veeqo line mirror. **This is exactly the divergence `print-divergence-watchdog` reconciles.**
- `v3.pnp_order_lines` — single writer `veeqo-order-sync.js:52` (status-rank guard prevents regression `:25,63-69`).

---

## 6. Printing

.28 label station + P&P print-divergence + image upload.

**Owning files (endpoints in `src/routes/op.js`)**
- `print-login:260`, `print-heartbeat:347`, `print/other:360`, `print-event:418` (→ `v3.print_jobs`), `print-watchdog:561`, `print-progress:584`, `printer-status:691`. Error→action translator `printerErrorAction:622`; issue regex `:617`.
- `src/workers/print-divergence-watchdog.js:19` — noon reconcile operator-typed vs Veeqo.
- `src/v3/images/router.js:49` — `POST /api/images/upload:67` (iPhone photos → Slack #images, no DB).
- `src/v3/print-stream.js:14` — SSE hub (`broadcast:21`). `src/v3/station-operator.js:20` — single source of "who is at .28 now".

**Tables read / written**
- `v3.print_jobs` — WRITE (dedup by day) `op.js:455`, physical close `:772,776`; READ `:484,756`.
- `v3.printer_status` — WRITE upsert `:715`; READ `:706,904`. `v3.printer_status_log` — WRITE on change `:727`.
- `v3.print_progress` — WRITE `:590`. `v3.print_station_login_log` — WRITE `:272`; READ `station-operator.js:24`.
- `v3.settings` (`print_station_operator`) — WRITE `:263` (login), `:351` (heartbeat); READ `:445`, `station-operator.js:35`.
- `v3.print_divergence_log` — WRITE `print-divergence-watchdog.js:80,111,131`; READ `:76,86,120`.
- `v3.events` — WRITE batch/desc sync `op.js:436,789`, open label task `:280`; READ `:410`, order_printing totals `print-divergence-watchdog.js:51`.
- `v3.operator_action_log` WRITE `:367`; `v3.audit_log` watchdog dedupe `:570,574`; `v3.product_batches`/`v3.products` READ `:387,404,752`.

**Receives from**
- Orders/Veeqo: `print-divergence-watchdog.js:58` calls `veeqo.shippedByDay()`, reads operator totals `v3.events.orders_printed:51`.
- Production/events: print-event reads open `label_printing` event `op.js:410` + product/batch tables.

**Sends to**
- Slack: admin-orin job notices `op.js:513,819`; watchdog `:577`; divergence question to orders channel `print-divergence-watchdog.js:99`; images → #images `images/router.js:21,115`.
- Dashboard SSE: `print-stream.broadcast` — `done:523`, `progress:609`, `printer:733`, `finished:824`.
- **To Stock (supplies):** intended `SupplyService.consumeForSize` at label print (`SupplyService.js:13`) — **not wired** (see Stock §UNCERTAIN + BROKEN LINKS).

**State location + duplicate writers**
- **`print_station_operator` setting written in TWO op.js places**: login `:263` + heartbeat `:351`. Per `station-operator.js:6-13`, this mutable setting is deliberately NOT the identity source — the append-only `v3.print_station_login_log` (`:272`) is authority (the setting is presence/heartbeat only). Station identity lives in two stores with the log as authority.
- Printer state in two tables: snapshot `v3.printer_status:715` + history `v3.printer_status_log:727`.
- `print_jobs.sheets` written first at spooler (`:455`) then overwritten with machine counter on physical finish (`:776`) — the "spooler lies for PDF" reconciliation.

---

## 7. Cameras

View-only PIN-gated proxy to the camera-PC gateway + machine-motion signal.

**Owning files**
- `src/routes/cameras.js` — entire proxy. Router `:35` (mounted `src/index.js:61`). PIN→token session `:104` (HMAC `:90-101`). MJPEG multiplex `:333`, broker `:204-295`, H.264 fMP4 `:302`. Pages `/cameras:541`, `/cameras/pip:366`, `/cameras/tag:412`. Machine-zone tagging `:124,134,155`. Gateway external via `CAM_TUNNEL_URL`/`CAM_TOKEN` (`:179,212,316`).
- `src/cameras-schedule.js` — schedule gate (`isCamerasOn`), imported `cameras.js:32`; weekend on-demand via `src/v3/workday.js` (`cameras.js:33`).
- `src/workers/encap-monitor.js` — consumer of the camera→machine signal: `_cameraSaysMoving:158-170`, applied in `tick:179`.

**Tables read / written**
- WRITE `v3.blocked_ips` — brute-force bans on `/api/cam` (`cameras.js:76,81` → `security.js:121`).
- READ/WRITE `v3.camera_zones` — SELECT `:127`, INSERT `:148`, deactivate `:159`. (A real owned table, not just proxy.)
- READ `v3.settings` key `machine_state` — NOT in cameras.js; read by `encap-monitor.js:160`. The row is WRITTEN by the .28 machinemon endpoint `POST /api/machine-state` `src/routes/op.js:538,546`.
- External streams are proxied, not stored.

**Receives from**
- Admin/security middleware: rate-limit + brute-force (`cameras.js:31,76-81`, source `src/middleware/security.js`).
- .28 machinemon: motion via `v3.settings 'machine_state'` (`op.js:546`) → encap-monitor `:160`.

**Sends to**
- Slack: brute-force ban alert via `src/v3/slack/sender` `postAs` (`cameras.js:53,76`).
- Encap-monitor: camera "moving" signal gates the operator alarm (`encap-monitor.js:167`); camera-sees-motion-but-no-task → ask admin instead of alarm (`:172-177`).

**State location**
- Session tokens are stateless HMAC (no DB, `cameras.js:90`); brokers in-memory (`:195`).
- `machine_state` in `v3.settings` — single writer `op.js:546` only; single reader `encap-monitor.js:160`.

---

## 8. Slack

**Owning files**
- V3 (active): `src/v3/slack/events-v2.js` — webhook `POST /slack/events-v2` (verify `:36`, `handleEvent:56`, router `:328`, wired `wire.js:96`). Reactions/interactivity `:65-183`; mute interception `:196-234`. `src/v3/slack/sender.js` — shared OUTPUT: `postAs:108`, `addReaction:206`, `updateMessage:226`, `postDm:244`.
- Legacy (`src/slack/`, parallel, writes legacy `app_state`/public schema, NOT `v3.*`; gated off by `V2_DISABLED`): `events.js`, `poller.js` (writes `app_state` `:34`), `client.js`, `interactive.js`, `home.js`, `admin-chat.js`, `dm-handler.js`, `options.js`.

**Tables read / written (V3)**
- WRITE `v3.messages` — INSERT new human msg `events-v2.js:283-287` (`llm_processed_at=NULL` for Observer); edit re-arm `:249`; delete mark `:259`.
- READ/WRITE `v3.pending_commands` — cancel on ❌ `:160`. READ/WRITE `v3.notifications` — saturday_idle `:80,88`, noclockin `:107,121`.
- READ `v3.persons` (admin gate `:132`); WRITE `v3.operator_sessions` (saturday checkout `:92`); READ `v3.events` (soft-delete linked `:262`).
- Legacy poller writes `app_state` (`poller.js:34`), NOT `v3.*` (confirmed: no `v3.messages/notifications/pending_commands` writes under `src/slack/`).

**Receives from**
- Admin (kill-switch/mute): `alertGate.parseMuteCommand` `events-v2.js:203`.
- AI (CommandHandler): reaction ✅ → `commandHandler.confirmAndExecute:150`; mention gate `:235`.
- NotificationHandler (dedupe): `handleReaction:144`.

**Sends to**
- **`sender.js` `postAs` is the shared Slack output for ALL workers/services** (encap `wire.js:574`, absence `:566`, CommandHandler `CommandHandler.js:932`, Observer `Observer.js:260`, NoteAnalyzer `wire.js:145`, cameras `cameras.js:53`). Single output module `src/v3/slack/sender.js`.
- To AI: writing `v3.messages` with `llm_processed_at=NULL` (`events-v2.js:285`) is the hand-off the Observer claims (`Observer.js:810`).

**State location + duplicate writers**
- `v3.messages` written by BOTH events-v2 (`:283`) AND Observer (updates `Observer.js:526,538,369,653`) — multi-writer (insert vs process-update, non-conflicting).
- Mute state = `v3.settings operator_alerts_muted_until`, written `alert-gate.js:74` (from events-v2 `:212`), cleared `:78`.

---

## 9. Admin

**Owning files**
- `src/routes/admin.js` — Admin Panel API `/api/adminpanel/*`. `createAdminRouter:57` (wired `wire.js:166`). HMAC session-token auth (8h cookie `hf_admin`): sign `:35`, verify `:39`. Login rate-limit `:31`.
- `src/v3/admin-v3/routes.js` — read-only shadow-inspection HTML `/api/admin/v3/*`. `createRouter:394` (wired `wire.js:131`), PIN auth `:49`, consumes shared read repos `:397`.
- `src/admin/app.js` — static admin UI (served `wire.js:172`).
- Auth middleware `src/middleware/security.js` (`securityHeaders`, `makeBruteForceGuard`, applied `wire.js:116-127`).

**Tables read / written**
- WRITE `v3.audit_log` — admin actions `admin.js:72-75` (`audit()`, actor_type='admin'). admin-v3 is read-only.
- WRITE `v3.blocked_ips` — via brute-force guard (`wire.js:126` → `security.js:121`).
- READ legacy `public.tasks`/`public.phase_instances` — divergences cross-ref `admin-v3/routes.js:167`.
- admin.js also manages `v3.persons`, `v3.operator_sessions`, `v3.notifications` per endpoint.

**Receives from**
- Slack sender: `createAdminRouter` receives `{ postAs, updateMessage }` `admin.js:59`.
- Shared read layer: admin-v3 uses same repos as data API (`admin-v3/routes.js:397`).

**Sends to**
- Slack: notification accept/edit via injected `slack.postAs`/`updateMessage` (`admin.js:59`).
- AI pipeline (indirect): mute state it governs is read by workers via `alert-gate.isMuted` (`encap-monitor.js:187`); the mute WRITE happens in the Slack path (`events-v2.js:212`), not admin.js.

**State location + duplicate writers**
- **`v3.audit_log` written by MANY (31 files)**: `admin.js:73`, `CommandHandler.js:949`, `Observer.js:233,604,658,733,750`, `EventService.js`, `GoalService.js`, `NotificationHandler.js`, `StockService.js`, `ProductionCountService.js`, `BatchService.js`, `DeadlineService.js`, `CatalogService.js`, `middleware/security.js`, `v3/ems-confirm.js`, `v3/wire.js`, most `src/workers/*`. (Append-only log — many writers is expected, not a conflict.)
- **`v3.settings` written by MANY distinct writers**: `machine_state` (`op.js:546`), `print_station_operator` (`op.js:263,351`), printer-incident (`op.js:883`), mute (`alert-gate.js:74`), workday plan (`workday.js:73`), Observer heartbeat/cooldown (`Observer.js:764,739`), `worker_tick_<name>` for ~20 workers (`wire.js:253` `beat()`), migration seeds. Each key has a distinct owner (key-scoped, not a conflict).

---

## 10. AI / LLM

**Owning files**
- `src/v3/llm/Observer.js` — pipeline orchestrator. Class `:47`; `_process:92` (pre-filter→PersonResolver→prompt-builder→classify→persist→react); worker loop `tick:789`, claim `:810`. Shadow-mode gate `:253`.
- `src/v3/llm/LLMProvider.js` — provider abstraction. `getProductionProvider:133` = Gemini-primary QuotaChain (Flash→Flash-Lite→extra keys→OpenRouter, `:146-164`); Anthropic explicit-only (`:117,137`). Header notes "Anthropic out, 100% Gemini" (`:127`).
- `src/v3/llm/note-analyzer.js` — `NoteAnalyzer:27`, `classifyRaw:48`, gated `NOTE_LLM_ENABLED:33`, wired `wire.js:145`.
- `src/v3/llm/prompt-builder.js` — read-only context builder, no LLM call (`:8-9`).
- `src/v3/services/PersonResolver.js` — `resolve:279` (LLM for shared accounts).
- `src/v3/services/CommandHandler.js` — admin @Carolina command parser/executor. `_parseCommand:271-403` (`classifyRaw:403`), `tryRoute:83`, `confirmAndExecute:188`.
- Legacy `src/ai/` (detect.js, note-classifier.js, persona.js, proposals.js, admin-tools.js) — separate from V3 path.
- Pre-filter (deterministic pre-LLM): `src/v3/llm/pre-filter.js` (`Observer.js:25`).

**Tables read / written**
- READ/WRITE `v3.messages` — claim `Observer.js:810-824`; finalize `:526`; skipped `:538`; coalesced `:471`. Verdict stored in `llm_result` **JSONB column** (`:527,540`).
- WRITE `v3.llm_metrics` — per-call telemetry `Observer.js:553-558`. (There is no standalone `v3.llm_result` table.)
- WRITE `v3.audit_log` — `Observer.js:233,604,658,733,750`; `CommandHandler.js:949`.
- WRITE `v3.pending_commands` — CommandHandler `:171-177`, status transitions `:210,227,244`.
- WRITE `v3.events` via EventService — Observer `:313`; CommandHandler `:503,595,782,798`. `v3.notifications` dead-letter `Observer.js:662`.
- READ `v3.persons`/`products`/`activity_types` (validation `:414`); READ `v3.settings` operational_window `:480`.

**Receives from**
- Slack: consumes `v3.messages` rows from `events-v2.js:283`, claims at `Observer.js:810`.
- Admin (settings): operational window `:480`, mute cooldown `:708`. Provider selection from env `LLMProvider.js:134`.

**Sends to**
- Slack via shared sender: Observer review/dead-letter/billing `Observer.js:254,260,674`; CommandHandler replies `:932` + reactions `:919`.
- Events domain: CommandHandler creates `v3.events` (`:503,595,782,798`); Observer via EventService (`:313`).
- Admin: writes `v3.audit_log` consumed by admin panels.

**State location + duplicate writers**
- LLM verdict = `llm_result` JSONB column on `v3.messages` (Observer only, `:527,540,472`).
- `v3.pending_commands` written by CommandHandler (`:171`) AND cancelled by events-v2 (`:160`) — multi-writer across AI+Slack (create vs cancel, non-conflicting).
- `v3.settings` heartbeat/cooldown keys written by Observer (`:739,764`) — part of the many-writer `v3.settings` set (§9).

---

## NOT REACHABLE

`.js` files under `src/` that no other `src/` file `require()`s. Method: basename-require scan from every non-test `src/*.js` (see session report). **Browser-served files are reachable via `<script>`/static even though nothing `require`s them** — those are marked [static], NOT dead.

**Reachable via browser `<script>` / static (NOT dead):**
- `src/op/app.js` (2332) — served `wire.js:161`, loaded `src/op/index.html`.
- `src/op/fuse-data.js` (2418) — `<script src="/op/fuse-data.js">` (`op/index.html:31`).
- `src/op/state-machine.js` (128) — `op/index.html:32`.
- `src/op/offline-queue.js` (58) — `op/index.html:33`.
- `src/op/sw.js` (54) — service worker for /op.
- `src/admin/app.js` (1078) — served `wire.js:172`.
- `src/shared/hf-design.js` (111) — `<script src="/shared/hf-design.js">` (`op/index.html:34`).
- `src/print/print.js` (155) — `<script src="/print/print.js">` (`print/index.html:30`).

**Reachable via directory-index require (NOT dead):**
- `src/db/index.js` (736) — `require('../db')` (e.g. `src/admin/audit.js:12`, many).
- `src/parser/index.js` (956) — `require('../parser')` (e.g. `src/routes/api.js:14`).

**Genuinely unreferenced (no require, no `<script>`, no static mount found):**
- `src/op/app.legacy.js` (745) — only referenced by `src/op/index.legacy.html`, which is not served by `wire.js`/`index.js`.
- `src/op/sw.legacy.js` (39) — same (legacy html only).
- `src/ai/note-classifier.js` (139) — no `require` found anywhere in `src/` (non-test).
- `src/analysis/compute-task-targets.js` (277) — no `require` found (non-test).
- `src/dispatcher/retro-operator.js` (23) — no `require` found (non-test).
- `src/v3/llm/providers/FallbackProvider.js` (66) — not in `getProvider`/`getProductionProvider` chain (`LLMProvider.js:115-164`); no `require` found (non-test).
- `src/v3/utils/idempotency.js` (11) — no `require` found (non-test).

(No deletions recommended — per instruction. Some may be used only by tests or kept as intentional spares.)

---

## BROKEN LINKS

State written in two places that could disagree, or edges emitted with no consumer:

1. **`v3.events.ended_at` — many independent writers, mostly guarded.** (Verified 2026-08-11 against code + schema; corrects an earlier draft of this claim.) The real direct writers of `v3.events.ended_at`:
   - `src/routes/op.js` — ~9 raw closes: `:1395,1419,1519` (machine_return), `:1540,1550` (overnight expiry), `:1662,1669,1680` (lunch/new-task), `:3388` (forgotten-checkout cascade).
   - `src/v3/services/EventService.js:211` (`_patch`, the only parameterized path).
   - `src/routes/admin.js:654,1187`; `src/v3/data/router.js:1860,1913`; `src/v3/services/CommandHandler.js:783,798`; `src/workers/attendance-sync.js:339,548,564`; `src/workers/ems-activity-sync.js:228,244`.
   - **Correction to the earlier draft:** `BatchService.js:159,195` do NOT write `ended_at` (they write `product_batch_id`). `src/admin/merge.js:74` and `src/workflow/engine.js:283,520` DO write `ended_at`, but on the **legacy `public.tasks`/`phase_instances`/`ad_hoc_task_instances` tables, NOT `v3.events`** — so they are out of scope for this claim.
   - **Guard reality:** most closers include `WHERE ended_at IS NULL` (attendance-sync `:339,548,564`; CommandHandler `:783,798`; op.js bulk closers), which makes a concurrent double-close a no-op. Split the rest:
     - **Genuinely unguarded** (close by id / `ANY(ids)` with NO `ended_at IS NULL` on the UPDATE): `op.js:1662,1669,1680`.
     - **TOCTOU window** (UPDATE by `id` alone, but the row was SELECTed with `ended_at IS NULL` immediately before, e.g. `op.js:1393` → `:1395`): `op.js:1395,1419,1519`. A narrow read-then-write race, not a naked double-close.
   - `EventService._patch` additionally has a **negative-duration guard** (`EventService.js:122-140`): if a patch would set `ended_at < started_at`, it clamps `ended_at = started_at` and audits.
   - **No DB-level guarantee:** `idx_events_active` (`migrations/001_v3_initial.sql:144`) is a **non-unique** partial index — it does NOT enforce "one open event per person." All protection is the app-level `WHERE ended_at IS NULL` clause.
   - Net: the `EventService.js:5-21` "single write-door" invariant is not enforced against op.js's raw SQL, but the practical race surface is small (only `op.js:1662,1669,1680` lack any guard).

2. **`v3.production_counts` — 6 writers, "one write path" comment overstated.** (Verified 2026-08-11.) The real writers: `src/routes/op.js:1132` (`insertCount`), `:1160` (`insertOrdersCount`), `:2250`, `:2912`; `src/routes/admin.js:1013`; `src/v3/services/ProductionCountService.js:87` (canonical, dedup). `insertCount`/`insertOrdersCount` write **raw SQL, bypassing ProductionCountService** — no dedup/supersede. The `wire.js:180` "one write path" holds only for the followup-total path (`recordTotal` → `ProductionCountService.record`), not the operator `/end` inserts. Risk: op.js bypasses the dedup/supersede logic that catches double-counting.

3. **`v3.stock_movements` — dual-path ledger.** `StockService._insertMovement` (idempotent, updates bin/box qty `:77`) vs raw kiosk INSERT `op.js:324` (source `'op_kiosk'`, no qty update, no idempotency). The header at `StockService.js:6` explicitly warns about this. Risk: kiosk picks that never adjust bin quantities.

4. **`SupplyService.consumeForSize` — emitted-by-design, no consumer.** The "print shipping label → deduct supply/envelope" edge is defined (`SupplyService.js:13,65`) but has **no production caller** — only `SupplyService.change` is wired (admin endpoint `router.js:1384`); `consumeForSize` appears only in tests. The label-print→supply-deduction loop is not closed.

5. **`production_counts kind='orders'` (operator-typed) vs Veeqo line mirror — divergent sources by design.** Orders count is operator-typed at print-task start (`op.js:1739`), while `veeqo-order-sync.js` writes only `v3.pnp_order_lines` (never `production_counts`). These two "how many orders today" numbers can disagree; `print-divergence-watchdog` (`:51,58`) exists precisely to reconcile them at noon. Working as intended, but it IS two sources of the same truth.

---

## UNCERTAIN

- **`v3.admin_chats`**: no writer found in the V3 LLM path. Likely does not exist as an AI-owned table; the admin-chat surface is legacy `src/slack/admin-chat.js`. Not confirmed either way.
- **`src/routes/admin.js` production_counts / product_batches / events writes**: cited from grep only (file not read in full by the tracers). Exact lines/semantics of admin adjustments are partially verified.
- **`src/v3/services/CommandHandler.js` and `src/v3/llm/Observer.js` event-write lines**: cross-referenced from grep; specific `ended_at` write lines in these two are asserted from the tracer's grep, not a full read.
- **Login-session creation** resolved to `src/lib/op-auth.js:43` (creates `v3.operator_sessions`) — confirmed in follow-up.
- The 7 "genuinely unreferenced" files in NOT REACHABLE were checked only for `require('...basename')` and `<script>`/static mounts; a file loaded by an unusual dynamic path or by a test-only harness could be mislabeled. Confidence high but not absolute.
