# UNCERTAINTIES — what could NOT be confidently determined

Nothing here was resolved by guessing. Each item lists affected IDs, what is known, what is unknown, conflicting evidence, and what would resolve it.

| U | Affected IDs | Known | Unknown | Conflicting evidence | To resolve |
|---|---|---|---|---|---|
| U-01 | S02.15.06, S03.06, S13.02, S11.03 | `/dashboard` mount exists (`wire.js:206`); `public/dashboard/` absent on disk and in git; `docs/ARCHITECTURE.md:33` lists it as live; last two drift runs said "no drift" | Whether the previous-gen SPA was ever built/committed, or intentionally removed; whether anything external still links `/dashboard/*` | ARCHITECTURE.md vs disk | Bruno confirms intent; then either remove mount + doc line, or restore build. Add mount-dir-exists check to `architecture-drift.test.js` |
| U-02 | S11.05 | Memory/Obsidian describe Simone PC `.246` as print-submit target (shipping labels, label footer Fase 2) | No code in `src/` references `.246`; how it connects today (if at all) | memory says "print-submit on SIMONE'S PC .246"; code only knows `.28` | Inspect `.246` machine / LAN Intercom project; confirm whether any script posts from `.246` |
| U-03 | S11.06, S11.04.01 | `README-carol.md:17` mentions a Windows Scheduled Task ("Ana") invoking `carolina-say-file.js` | Whether it exists/still runs; on which machine | none in repo | `Get-ScheduledTask` on Bruno's PC |
| U-04 | S11.07 | Memory: LAN Intercom is a separate project (`c:\Claude Projects\LAN Intercom`) | Any runtime coupling with this service | none | Read that repo's README |
| U-05 | S04.29, S04.01, S04.05, S04.19, S04.20, S04.23, S04.28.03, S04.28.04, S04.25 | Registry claims watchdog 60s (code: 10 min); note_analyzer has no `enabledEnv`; 4 destructive/critical inline timers + all V2 crons absent from registry | Whether registry is meant to be exhaustive (its header says yes) | `process-registry.js:2-31` vs `wire.js` | Decide scope of registry; add missing entries or narrow its claim |
| U-06 | S04.06, S04.21 | Heartbeats are separate `setInterval`s, not tied to worker ticks | Whether this is intentional ("liveness of process") or a bug | comment absent | Bruno decides; if bug, wire `heartbeat` dep into both workers |
| U-07 | S08.06 | 9 tables written in prod exist only in `scripts/create-*.js` or nowhere | Whether the intent is to backfill migrations 071+ | CLAUDE.md "migrations are single source of truth" | Write migrations for them (needs confirmation; touches schema) |
| U-08 | S08.02 | No migration runner in `src/`; 019 missing; no `.down` from 034 | How v3 migrations get applied on deploy (out-of-band `scripts/v3-apply-migration-*`?) | CLAUDE.md claims schema truth | Confirm the apply procedure with Bruno; document in DEPLOY.md |
| U-09 | S03.01.07 | `OtherPages.jsx` bundles `#suporte`, `#produto`, `#falar`, `#planejamento`, `#carolina` | Exact contents/status of each (some may be placeholders) | not fully read | Read `OtherPages.jsx` fully |
| U-10 | S03.05 | 7 legacy pages still reachable, 3 shadowed | Whether anyone still uses `/admin/silent-log`, `/admin/carolina-config`, `/admin/workflows`, `/admin/ad-hoc-tasks`, `/operator/:id`; whether their features exist in v4 | GENERATIONS G-D1 | Access logs / Bruno; feature-parity check vs S03.01 |
| U-11 | S07, S06 | Both Carolina generations live; `V2_DISABLED` only stops crons; parser→dispatcher→engine still fires from `/slack/events` interactivity | Whether V2 write path still produces meaningful data anyone reads (`public.*` dashboards) | INTEGRATION_PLAN "E8 switch" done for UI, not for backend | Query recent `public.tasks`/`phase_instances` write dates (needs read-only DB — not done this session) |
| U-12 | S02.08, S02.09, S05.01, S05.08 | Raw writes bypass EventService/StockService | Whether every raw write is deliberate (some are commented as authorized bulk-FK) | service headers claim sole ownership | Per-write review; documented in ARCHITECTURE.md BROKEN LINKS |
| U-13 | S12.01 | `ADMIN_PIN` default `510510` in 4 places | Whether env is set in Railway (cannot read env) | — | Check Railway variables (Bruno) |
| U-14 | S08.05.08a `admin_chats` | **RESOLVED (2nd pass): NO writer in `src/`** (only `v3-migration.test.js:17` asserts existence; `slack/admin-chat.js` writes `public.pending_disambiguation`) → reclassified ORPHANED | Whether any out-of-repo script populates it | — | Bruno confirms it can be dropped in a future migration |
| U-15 | S08.05.09a `carolina_*` (017) | **RESOLVED (2nd pass): NO writer for any of the six**; the "Config Carolina" panel writes `public.app_state`/`message_variations` → 6 ORPHANED tables | Whether they were meant for a never-shipped V3 personality system | migration 017 exists; no consumer | Bruno decides drop vs build |
| U-16 | S11.01.05 `print.config.json` | Referenced by .28 scripts | Contents (dashboard_url, token) | not in repo | Read on `.28` |
| U-17 | S02.15.01 `/archive` | Created by Dockerfile; gitignored | Whether EOD screenshots still land there (V2 EOD gated by V2_DISABLED — is it set?) | — | Check Railway env `V2_DISABLED` |
| U-18 | S10.06, S10.07 | OpenRouter/Anthropic wired but conditional | Whether keys are set (i.e., whether these are live rungs) | — | Railway env |
| U-19 | S03.01.04, S02.08 stock, S05.08 | Two UIs (v4 inventory pages, /op stock kiosk behind `STOCK_UI_ENABLED`) over one service + one raw path | Which is the intended final inventory UI (Bruno's "final inventory pages" goal) | GENERATIONS G-I | Bruno decision |
| U-20 | S13.02, docs/architecture | Drift test guards ARCHITECTURE.md only | Whether to extend enforcement to STRUCTURE_INDEX / map (RULE #2) | — | Add a test that new routes/workers/tables must appear in STRUCTURE_INDEX.md (docs-only + test file; allowed) |
| U-21 | S01.04.01 `.env.example` | File exists (672 B) | Its contents — **unreadable this session (path denied by permission settings)**; cannot diff against the code-derived env list | — | Read it in a session with access; reconcile with `MASTER_SYSTEM_MAP.md` §Env |
| U-22 | S13.09 `.claude/scheduled_tasks.lock` | File exists next to `.claude/settings.json` | What scheduler owns it (Claude Code scheduled agents? relation to S11.03 drift task?) | — | Inspect file + Claude Code config |
| U-23 | S03.01.19, S12 | `public/dashboard-v4/assets/index-*.js.map` is served publicly (no auth on `/dashboard-v4`) | Whether shipping the source map is intended | — | Bruno decides (finding, not fixed: docs-only session) |
| U-24 | S02.08, S02.09, S06.17 (R143, R145) | op.js and admin.js resolve the production channel from their OWN env read, not from wire's `productionChannelId` | Whether the two defaults could diverge (latent split-brain) | wire passes only `adminChannelId` to both routers | Bruno decides whether to pass `productionChannelId` from wire (code change) |
| U-25 | S04.04 (R142) | forgotten-dm receives `ordersChannel = V3_ORDERS_CHANNEL` with no default | Whether the env is set in Railway; if not, that posting path fails silently | — | Check Railway env |
| U-32 | S15.13, S10.03 | Two inventories exist: Veeqo's per-SKU stock (decremented by Veeqo on ship) and OUR shelf/box ledger (mirrors the ship). Later our system must ADD to Veeqo (after production) and REMOVE (adjustments) via `veeqo-api.setStock` (warehouse 108841). | Drift between the two totals is inevitable without a reconciliation job (nightly compare per SKU/product → admin alert); who is authoritative when they differ | `setStock` exists but no reconciliation exists | **DECIDED 08-18: reconciliation ALERTS only — nothing auto-overwrites either side**; a human decides. Our ledger = physical truth, Veeqo = sales truth |
| U-26 | S08.05.03a `product_catalog` | Read by data router; written only by `scripts/import-supplement-catalog.js` | Whether that is the intended lifecycle (import-only) | — | Bruno confirms |
| U-27 | S15.06, S15.07, S15.12(a) | **DECIDED (Bruno 08-18):** only ADMINS/MANAGERS enter quantity into the warehouse; operators may propose changes but every operator change waits for admin approval before taking effect. Deduction on print = Veeqo's shipment event mirrored into our ledger (`STOCK_DEDUCT_MODE=live`), our system never writes to Veeqo on print. | **Nuance DECIDED 08-18:** shelf↔box moves (total unchanged) apply immediately with a log; ADD/REMOVE from product total needs approval; RETURNS need approval before re-entering stock; DAMAGED/issue bottles go to the secondary per-product bucket (`v3.stock_issues`, exists). Only open: `STOCK_DEDUCT_MODE` value in Railway (cannot read env) | — | check Railway env when SKU map confirmed |
| U-28 | S15.07, S15.08, S15.12(e) | **DECIDED (Bruno 08-18): Veeqo is the ONLY 'label printed' signal for now** (TikTok later). One idempotency key per order line (`veeqo_ship` + order:line) — already how `veeqo-order-sync`/`StockService.pick` work. The .28 physical print event is NOT a stock signal. | none | — |
| U-29 | S15.12(d) | **DECIDED (Bruno 08-18): reservation is per PRODUCT.** Shelf qty + box qty = product total (the control number); shelf/box are only for physical organization (shelf ≈ 48 bottles max, box > 110). Recommendation adopted: reserved = open (unshipped) Veeqo order lines already mirrored in `pnp_order_lines` → available = total − reserved (derived, no new state). | none | — |
| U-30 | S15.11, S15.12(e) | **DECIDED (Bruno 08-18): deduct shelf first, box second**; manual adjustments on either side are fine as long as shelf + box always equals the product total. | Dedicated operator page vs /op panel still open (design, not model) | — | Bruno picks when the operator page is designed |
| U-31 | S15.10 | **DECIDED (Bruno 08-18): forget Amazon/eBay/Walmart/TikTok inventory for now** — focus on organizing the actual warehouse inventory; channel pages come later. | Placement of "Planejamento" / "Produto": traced 08-18 — Planejamento is a PLACEHOLDER (never built; production planning idea), Produto is a PRODUCTION-per-product view + catalog list. **DECIDED 08-18: both stay UNTOUCHED and move to Operação next to "Metas"** (planning/product belong with goals, not inventory or P&P) | `OtherPages.jsx:516-560,677` | — |

